// Mobile namespace re-export — see /m/auth/login/route.ts for rationale.
// SSE feed for live cache invalidation in the Flutter admin app.
export { GET, runtime, dynamic } from "@/app/api/v1/sync/stream/route";
